unpriv_user {
	owprovdr
}

user_exec_check_args {
	# The test will try to execute this with a 0, so we should see "insufficient privileges"
	/userexectest-689acb0e1ec89f45-7085a2a1780f5f42/safebin/userexecprog "1" * @ nonesuch
}
