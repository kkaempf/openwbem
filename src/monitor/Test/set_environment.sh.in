#!/bin/sh

OW_PRIVMAN_NO_ABORT=1
export OW_PRIVMAN_NO_ABORT

BLOCXX_LIB_DIR="@BLOCXX_LIB_DIR@"

LD_LIBRARY_PATH="`pwd`/..:`pwd`/../../common"${TEST_LIBRARY_PATH:+:}${TEST_LIBRARY_PATH:-}${BLOCXX_LIB_DIR:+:}${BLOCXX_LIB_DIR:-}${LD_LIBRARY_PATH:+:}${LD_LIBRARY_PATH:-}

# AIX
LIBPATH=$LD_LIBRARY_PATH${LIBPATH:+:}${LIBPATH:-}

# HP-UX
SHLIB_PATH=$LD_LIBRARY_PATH${SHLIB_PATH:+:}${SHLIB_PATH:-}

# DARWIN (OSX)
DYLD_LIBRARY_PATH=$LD_LIBRARY_PATH${DYLD_LIBRARY_PATH:+:}${DYLD_LIBRARY_PATH:-}

export LD_LIBRARY_PATH LIBPATH SHLIB_PATH DYLD_LIBRARY_PATH
